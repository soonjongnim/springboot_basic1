<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.soon9086.basic1.boundedContext.member.mapper.MemberMapper">

    <insert id="insertMember" parameterType="com.soon9086.basic1.boundedContext.member.dto.MemberDTO">
        INSERT INTO member (<if test="providerId != null">provider_id, </if>username, email, password, provider, created_at)
        VALUES (<if test="providerId != null">#{providerId}, </if>#{username}, #{email}, #{password}, #{provider}, NOW());
    </insert>

    <select id="findByUsername" resultType="com.soon9086.basic1.boundedContext.member.dto.MemberDTO">
        SELECT *
        FROM member
        WHERE username = #{username}
            LIMIT 1;
    </select>

    <select id="findByEmail" resultType="com.soon9086.basic1.boundedContext.member.dto.MemberDTO">
        SELECT *
        FROM member
        WHERE email = #{email}
            LIMIT 1;
    </select>

    <select id="findById" resultType="com.soon9086.basic1.boundedContext.member.dto.MemberDTO" parameterType="long">
        SELECT id, username, email, password, phone, created_at
        FROM member
        WHERE id = #{id};
    </select>

    <select id="findByProviderId" resultType="com.soon9086.basic1.boundedContext.member.dto.MemberDTO" parameterType="String">
        SELECT id, provider_id, username, email, password, phone, created_at
        FROM member
        WHERE provider_id = #{providerId};
    </select>

</mapper>
